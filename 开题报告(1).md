## 基于自研LLM与混淆集增强的中文拼写纠错模型构建

---

### 一、选题依据与任务定位

本课程作业的核心要求是“从零构建一个大语言模型”，并完成预训练、后训练及评测。我们选择中文拼写纠错作为下游验证任务，理由如下：

- 中文纠错任务有明确的输入（错句）和输出（正句），有公开的标准评测集（如SIGHAN），可以用精确率、召回率、F1值来量化结果，方便验收。
- 纠错任务既需要语言基础能力（预训练阶段获得），也需要跟随指令修正错误（后训练阶段强化），能够完整检验我们搭建的模型流水线是否有效。
- 有丰富且优质的CSC数据集，也容易通过规则自动构造含错训练数据。

**我们的定位是“构建型”**：核心工作是亲手实现一个大语言模型的完整训练流程，而纠错是我们验证模型效果的下游方案。后续如果有时间可以写前端进行美化，逐步靠近应用型。

---

### 二、任务定义与形式化描述

**输入**：包含拼写错误的句子 \(X = (x_1, x_2, ..., x_n)\)

**输出**：修正后的正确句子 \(Y = (y_1, y_2, ..., y_n)\)

**示例**：

- 输入：我今天去公园跑不了。（“跑不了”应为“跑步了”）
- 输出：我今天去公园跑步了。

模型本质是学习一个条件生成映射 \(P(Y|X)\)，在保证语义流畅的前提下，精准定位并修正错字。

---

### 三、实现步骤

整个项目可以划分为四个需要我们实现的层面：数据准备、模型结构、训练策略、推理约束。

#### 3.1 数据准备

**目标**：准备预训练数据、混淆集、以及带指令的微调数据。

**预训练语料**：从公开渠道（如维基百科dump、新闻语料）获取中文文本，清洗后用于因果语言模型预训练，让模型先学会“正常说话”。

**混淆集构建**：
我们主要整理两类高频混淆对：

- **音近混淆**：利用`imewlconverter`提取拼音输入法中的同音/近音字对，并进一步覆盖前后鼻音不分（`in`↔`ing`）、平翘舌音不分（`z`↔`zh`）、`l/n`不分等方言发音特征。
- **形近混淆**：利用`HomoglyphsCJK`项目，整理字形结构相似的汉字对。

混淆集最终整理成一个字对映射表，同时为每个字建立“可能被误写成的候选字列表”，方便后续训练和推理中直接调用。

**含错训练数据构造**：
我们从干净的文本出发，用脚本自动生成错误句子：

- 随机选取句子中一定比例（如10%）的位置作为修改点；
- 对每个修改点，优先从该字对应的混淆候选集中随机选一个替代字，而非在全词表均匀采样。这确保训练数据中的错误分布贴近真实输入场景；
- 数据格式为标准自回归生成对：输入=错误句子，目标=正确句子。

**指令微调数据**：
根据老师建议，我们对错误进行了详细的分类，并在训练样本中加入类型标签。例如：

- `[音近-读音相同] 昨天他们去爬了长成。` → `昨天他们去爬了长成。`
- `[音近-前后鼻音] 他今天很开兴。` → `他今天很开心。`
- `[音近-l/n不分] 我现在非常蓝受。` → `我现在非常难受。`
- `[形近] 这里有一片高梁田。` → `这里有一片高粱田。`

分类尽可能完备，覆盖我们混淆集中实际存在的错误类型，保证每类都有足够样本。

#### 3.2 模型架构：基于Nano-Llama从零搭建

这是我们工作的主体工程。我们基于Nano-Llama轻量架构，从零实现一个参数量≤20M的生成式语言模型。各组件如下：


| 组件                      | 设计                      | 说明                                                                              |
| ------------------------- | ------------------------- | --------------------------------------------------------------------------------- |
| **Tokenizer**             | 字级别词表，大小约8000    | 覆盖常用汉字，同时显式纳入混淆集中出现的所有字，确保训练和推理时不会遇到OOV       |
| **Embedding层**           | 字嵌入 + RoPE旋转位置编码 | 替代绝对位置编码，提升位置表征能力和序列长度泛化性                                |
| **Causal Self-Attention** | 多头因果注意力            | 自回归掩码，每个位置只能看到上文；实现上尝试融入Flash Attention思想以降低显存占用 |
| **FFN**                   | 前馈网络 + SwiGLU激活     | 替代传统ReLU/GELU，在小参数量下提供更强的非线性表达能力                           |
| **归一化**                | RMSNorm                   | 替代LayerNorm，降低计算开销，有助于训练稳定                                       |
| **生成头**                | 线性映射 + Softmax        | 输出词表概率分布；推理阶段额外叠加混淆集掩码（详见3.4）                           |

选择小模型的原因是：小模型训练快、迭代快，适合我们有限的计算资源。

#### 3.3 训练策略：混淆集如何介入模型学习

混淆集不仅仅用来构造数据，而是贯穿训练全过程，让模型真正学会区分混淆字对。

**策略1：数据采样偏向混淆集**
在预训练/微调阶段构造训练batch时，修改点上的替代字优先从混淆集中采样，而非从全词表均匀随机选取。这提高了混淆字在训练样本中的曝光比例，引导模型将有限的参数容量聚焦于高频易错对。

**策略2：损失函数加权**

在标准自回归交叉熵损失的基础上，我们增加两个机制：

- **混淆字区分辅助损失**：当序列中出现混淆集内的字时，给该位置的损失值乘以更高的惩罚权重（如1.5~2.0），强制模型更谨慎地处理这些易混淆位置。
- **正确位置逐token拷贝加权**：对句子中原有的正确字位置，适当提高保留原字的奖励信号，抑制模型的过度修改倾向。

**策略3：高效训练配置**

两台电脑的资源约束下，我们采用：

- **AMP BF16混合精度**：减少显存/内存占用，加速训练；
- **梯度累加**：在物理batch size受限的情况下，通过累加多个小batch的梯度来模拟更大的有效batch，稳定训练过程。

#### 3.4 推理约束：混淆集受限解码

推理阶段，模型自回归逐字生成。普通做法是从全词表做Softmax采样，而我们额外叠加混淆集掩码：

- 对输入句子中的每个位置，查找该位置原字的混淆候选集（音近+形近候选字列表）；
- 将模型输出的logits在候选集以外的字上置为`-inf`，再Softmax归一化；
- 模型只能在原字及其混淆候选中选择替换，不能生成不相干的字。

这一步从根本上抑制过纠——候选集从8000词表缩小到通常只有个位数，既提升了纠错精准度，也大幅加快了推理速度，是我们实现<50ms/句延迟的关键保障。

#### 3.5 评测

- **预训练评测**：困惑度（Perplexity）监控训练收敛；人工抽检生成文本是否通顺。
- **纠错能力评测**：在SIGHAN测试集或我们划分的held-out数据上，报告检测P/R/F1和纠正P/R/F1；对比“无混淆集约束”与“有混淆集约束”的过纠率差异。
- **消融实验**：逐项验证混淆集各策略（数据采样偏向、损失加权、受限解码）的增益。如果某项策略效果不明显，我们如实记录并分析原因，不强行美化数据。

---
### 四、错误分类体系与指令数据构建

#### 4.1 错误分类体系设计

为提升模型对不同类型拼写错误的识别与纠正能力，我们不再将中文拼写纠错简单建模为“错字→正字”的映射问题，而是对错误进行结构化建模，构建细粒度错误分类体系，并将其作为后续数据构建与模型训练的重要依据。

结合中文拼写错误的实际分布情况，将错误划分为两大类，并进一步细化为可操作的子类别，同时为每一类错误分配统一标签，便于程序化处理与模型学习：

| 一级类别 | 子类别       | 标签           | 示例              |
| -------- | ------------ | -------------- | ----------------- |
| 音近错误 | 前后鼻音混淆 | PHONETIC_AN    | 今→经             |
| 音近错误 | l/n不分      | PHONETIC_LN    | 蓝→难             |
| 音近错误 | 平翘舌混淆   | PHONETIC_RETRO | 是→四             |
| 音近错误 | 声调混淆     | PHONETIC_TONE  | 行(xíng)→行(háng) |
| 形近错误 | 字形相似     | SHAPE_SIM      | 已→己             |

优点：

1. 覆盖中文拼写纠错中的高频错误类型
2. 每一类均可通过规则或资源实现自动构造
3. 标签化设计便于后续模型进行条件学习或多任务学习

------

#### 4.2 基于分类的错误数据自动构建方法

为了避免人工标注成本过高的问题，本文采用“规则驱动 + 混淆集支持”的方式，对干净语料进行自动错误注入，从而构建大规模纠错训练数据。

##### 4.2.1 原始语料准备

选取公开中文语料（如新闻文本、百科语料等）作为“正确句子”，记为目标输出 YYY。

##### 4.2.2 错误注入策略

针对不同错误类型，设计对应的自动化生成规则：

1、音近错误生成

基于拼音映射规则与输入法混淆集，对句子中的部分字符进行替换：

- 构建拼音级混淆规则，例如：
  - an ↔ ang
  - l ↔ n
  - zh ↔ z
- 实现方式：
  1. 将句子转为拼音序列
  2. 根据规则随机选择可替换位置
  3. 从混淆集中采样替换字符

示例：
 正确句：今天心情很好
 错误句：今天心情很号

（2）形近错误生成

利用字形相似字典（如 HomoglyphsCJK）或人工整理的形近字表，对字符进行替换：

示例：
 正确句：已经完成任务
 错误句：以经完成任务


##### 4.2.3 基于概率的错误采样

为提高生成数据的真实性，避免完全随机替换带来的分布偏差，引入概率控制机制：

- 为每类错误设置采样概率，例如：
  - 音近错误：60%
  - 形近错误：40%
- 在混淆集中进一步设置替换概率（如“的/地/得”分布）

该策略可使生成数据更接近真实用户输入错误分布。


##### 4.2.4 数据构建流程

整体数据构建流程如下：

1. 输入干净语料句子 YYY
2. 随机选择错误类型 ttt
3. 根据对应规则生成错误句子 XXX
4. 记录三元组 (t,X,Y)(t, X, Y)(t,X,Y)

最终得到结构化训练数据，用于后续模型训练。


#### 4.3 指令微调数据设计

在生成训练数据的基础上，进一步将纠错任务转化为指令学习问题，使模型能够在不同错误类型下学习差异化纠错策略。

##### 4.3.1 指令格式设计

每条训练样本由“指令 + 输入 + 输出”构成，基本形式如下：

**形式一：显式错误类型指令**

```
任务：中文拼写纠错
错误类型：PHONETIC_AN
请纠正句子中的错误：
今天心情很号
```

输出：

```
今天心情很好
```

**形式二：自然语言描述指令**

```
下面句子中存在由于前后鼻音混淆导致的错误，请进行纠正：
今天心情很号
```

**形式三：通用纠错指令（无类型）**

```
请对下面句子进行拼写纠错，并尽量减少不必要修改：
今天心情很号
```


##### 4.3.2 多样化指令策略

为避免模型过拟合单一表达方式，本文采用多模板混合策略：

- 显式标签指令（增强可控性）
- 自然语言指令（增强泛化能力）
- 无标签指令（贴近真实应用）


#### 4.3.3 输入输出建模方式

模型最终输入形式为：

```
[Instruction] + [Error Sentence]
```

输出为：

```
Correct Sentence
```


### 4.4 可实现性分析

本方案具有良好的可落地性，主要体现在：

1. 错误生成规则简单，可通过 Python 脚本实现
2. 混淆集资源已有开源工具支持（如输入法词库、形近字库）
3. 指令数据构建无需人工标注，可自动批量生成
4. 可在有限计算资源下完成数据构建与模型训练

---

### 五、进度安排与分工


| 阶段                        | 时间（第几周） | 主要工作                                              | 负责成员       |
| --------------------------- | -------------- | ----------------------------------------------------- | -------------- |
| 文献调研与数据收集          | 1-2            | 收集语料、公开数据集；整理混淆集                      | 张斐枫、薛婷妤 |
| Tokenizer训练与模型代码实现 | 3-4            | 训练字级Tokenizer；实现Nano-Llama各组件；搭建训练框架 | 张斐枫         |
| 预训练                      | 5-6            | 在通用语料上训练基础模型；监控PPL                     | 薛婷妤         |
| 指令数据构造与微调          | 7              | 基于混淆集和分类，自动构造指令数据；进行SFT           | 张斐枫、薛婷妤 |
| 评测与消融实验              | 8              | 在CSC测试集上评测；完成消融对比；撰写分析             | 张斐枫、薛婷妤 |
| 报告撰写与答辩准备          | 9-10           | 整理结果，撰写最终报告，准备答辩PPT                   | 张斐枫、薛婷妤 |


---

### 六、预期成果

**预期成果**：

1. 一个完整可运行的中文拼写纠错模型（参数量≤20M），包含Tokenizer、模型权重、训练/推理代码，代码开源可复现。
2. 在SIGHAN等测试集上的基础纠错评测结果，以及混淆集各策略的消融对比数据。
3. 一套可复用的混淆集构建代码及指令数据生成pipeline。
4. 详细的技术报告，分析模型优缺点及改进方向。

**可能遇到的问题以及应对方法**：

- **训练时间不足**：若训练过慢，我们会进一步缩小模型规模（如参数量降至10M），或减少预训练数据量，优先保障流水线完整。
- **微调后纠错效果不佳**：我们会分析是数据问题、模型容量问题还是任务设定问题，并在报告中详细讨论经验与教训。
- **混淆集增强无显著效果**：分析可能原因（例如小模型容量不足以区分细微混淆）。


### 七、总结

本项目以“生成式LLM+先验知识融合”为核心思路，通过构建多维混淆集与细粒度错误分类体系，探索中文拼写纠错任务中模型能力与语言知识的深度融合路径。

1. **亲手实现LLM完整流程**：从Tokenizer到Nano-Llama各组件，从预训练到受限解码，全部自己编码，扎实掌握大模型构建的每个环节。
2. **混淆集的系统性介入**：不像普通纠错模型只在数据构造时用混淆集，我们在数据采样、损失加权、推理解码三个层面都让混淆集发挥作用，形成闭环约束。
3. **细粒度错误分类与指令数据**：老师建议的“细致分类错误”我们落实为自动构造pipeline，在前缀标签中加入错误类型信息，让模型学会针对不同错误模式采用不同的修正策略。分类不求学术完备，只求真实验覆盖高频错误。
