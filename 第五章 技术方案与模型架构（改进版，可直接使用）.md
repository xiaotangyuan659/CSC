# 第五章 技术方案与模型架构（改进版，可直接使用）

## 5.1 整体技术路线

在原有PPT中“数据准备 → 模型构建 → 预训练与后训练 → 评估”的基础上，本文进一步细化技术流程，使其与第四章的错误分类与数据构建方法形成闭环。

整体流程如下：

1. 构建干净语料与混淆集资源
2. 基于错误分类体系进行自动错误注入，生成纠错数据
3. 构建指令格式训练样本
4. 训练轻量级生成式语言模型（预训练 + 指令微调）
5. 在标准数据集上进行评估与对比实验

该流程的核心在于：**将错误分类信息贯穿数据构建与模型训练全过程**。

------

## 5.2 混淆集构建与使用方式

你们PPT里已经提到了混淆集，这里要补的是：**不仅构建，还要说明“怎么用”**。

### 5.2.1 混淆集构建

基于已有工具与资源构建两类混淆集：

- **音近混淆集**
   利用输入法词库（如 imewlconverter）提取同音或近音字
- **形近混淆集**
   利用字形相似工具（如 HomoglyphsCJK）或人工整理字表

同时对常见错误（如“的/地/得”、“在/再”）进行人工补充。

------

### 5.2.2 混淆集在系统中的作用（关键要写出来）

混淆集在本项目中用于两个阶段：

#### （1）数据构建阶段（主要用途）

- 用于生成音近/形近错误
- 提供候选替换集合

#### （2）模型推理阶段（可选增强）

- 对模型输出进行约束或过滤
- 限制修改范围，降低过纠率

👉 这里写一句加分的话：

> “通过将混淆集同时用于数据构建与推理约束，实现先验知识在训练与推理阶段的一致性利用。”

------

## 5.3 模型选择与轻量化实现

你们目标是 ≤20M 参数，这里不要写太复杂模型，建议**务实一点**。

### 5.3.1 模型选择

本文采用**轻量级生成式模型**作为基础模型，具体实现可选：

- 小规模 Transformer Encoder-Decoder
   或
- 精简版 GPT（小层数、小hidden size）

输入输出形式：

```
输入：指令 + 错误句
输出：纠正后的句子
```

------

### 5.3.2 轻量化策略（一定要写，不然20M不可信）

为满足参数与延迟要求，采用以下策略：

- 降低层数（如 4~6 层）
- 控制隐藏维度（如 256~512）
- 限制词表大小（仅保留常用汉字）
- 使用共享embedding（输入输出共享）

👉 这些都非常容易实现（用 HuggingFace / PyTorch 都能做）

------

## 5.4 训练策略设计（重点强化）

这一部分是你们原来写得比较“虚”的地方，这里帮你们落地。

------

### 5.4.1 预训练阶段（对应PPT）

目标：让模型具备基础语言建模能力

训练数据：

- 自动构造的“含错误句子”

训练方式：

- 标准语言建模（预测下一个token）

👉 可以简单写，不用复杂

------

### 5.4.2 指令微调阶段（核心改进）

这一部分要明确写：**你们的创新是怎么用起来的**

训练数据来自第四章构建的：

```
(错误类型, 错误句, 正确句)
```

训练输入：

```
[Instruction + 错误句]
```

训练目标：

```
生成正确句子
```

------

### 5.4.3 分类信息的使用方式（必须写清楚）

本文设计两种可实现方式：

#### ✅ 方法一：显式指令方式（推荐主方法）

将错误类型直接写入指令中：

```
错误类型：PHONETIC_AN
句子：今天心情很号
```

👉 优点：

- 实现简单
- 与LLM指令微调兼容
- 不需要改模型结构

------

#### ✅ 方法二：无类型指令（用于对比实验）

```
请纠正句子：
今天心情很号
```

👉 用来做对比实验，验证分类是否有效

------

👉 写一句总结（老师很爱看）：

> “通过对比‘显式错误类型指令’与‘无类型指令’，验证错误分类信息对模型性能的影响。”

------

### 5.4.4 损失函数（简单但要写）

采用标准交叉熵损失：

```
Loss = 生成句子与目标句子的差异
```

（不要写复杂，多任务如果你们不确定能做就不要写）

------

## 5.5 推理与后处理策略

这一部分你们原来没有写，可以简单加一点（会加分）。

### 5.5.1 推理过程

输入：

```
指令 + 错误句
```

输出：

```
纠正句
```

------

### 5.5.2 基于混淆集的简单约束（可实现）

为降低过纠率，可以加入简单规则：

- 仅允许修改属于混淆集的字符
- 若修改不在混淆集中，则保留原字符

👉 这是一个很容易实现的小trick，但很加分

------

## 5.6 实验设计与评估方案（强化版，对应PPT第6页）

你们PPT这一页比较空，这里直接补强。

------

### 5.6.1 对比模型

- 基线模型：
  - BERT（无混淆集）
  - pycorrector
- 本文模型：
  - LLM（无混淆集）
  - LLM + 混淆集
  - LLM + 指令微调
  - LLM + 分类指令（最终模型）

------

### 5.6.2 消融实验

用于验证各模块贡献：

| 实验设置           | 说明           |
| ------------------ | -------------- |
| w/o confusion set  | 不使用混淆集   |
| w/o instruction    | 不使用指令     |
| w/o classification | 不使用错误分类 |

------

### 5.6.3 评估指标

- 检测指标：Precision / Recall / F1
- 纠正指标：Precision / Recall / F1

------

### 5.6.4 分类型评估（你们创新点的关键证明）

在不同错误类型上分别评估模型性能：

| 错误类型 | F1   |
| -------- | ---- |
| 音近错误 |      |
| 形近错误 |      |
| 语义错误 |      |
| 结构错误 |      |

👉 这一点非常重要，直接体现你们“分类有没有用”。

------

## 5.7 小结

本章在原有技术方案基础上进行了细化，将“错误分类 + 混淆集 + 指令微调”三者进行统一设计，形成完整的技术闭环。整体方法具有实现简单、资源需求低、可扩展性强的特点，适合在课程项目周期内完成。